const guests = 'guests';
const contributors = 'contributors';
const editors = 'editors';
const managers = 'managers';
const admins = 'admins';

export { guests, contributors, editors, admins, managers };
const roles = { guests, contributors, editors, admins, managers };
export default roles;
